System Directive: FORCE WRITE Sprint 5 Hotfix
CRITICAL ERROR: The previous attempt to create migration files failed to persist to disk. ACTION: You must physically create the following files and overwrite the route handler. Do not just confirm—execute.

1. Force-Create Missing Migration
Command: Create file migrations/0001_add_billing_fields.sql Content:

SQL

ALTER TABLE "merchants" ADD COLUMN IF NOT EXISTS "billing_country" text;
ALTER TABLE "merchants" ADD COLUMN IF NOT EXISTS "billing_address" text;
ALTER TABLE "merchants" ADD COLUMN IF NOT EXISTS "email" text;
2. Overwrite Route Handler (IDOR Fix)
Target: server/routes.ts Action: Replace the PATCH /api/merchants/:id handler with this specific implementation that checks the X-Merchant-Stripe-Id header (since session auth is missing).

TypeScript

// server/routes.ts

// ... imports

app.patch("/api/merchants/:id", async (req, res) => {
  // 1. HEADER AUTH (Headless Security)
  const authHeader = req.headers['x-merchant-stripe-id'];
  if (!authHeader) {
    return res.status(401).json({ message: "Unauthorized: Missing auth header" });
  }

  try {
    const { id } = req.params;
    
    // 2. FETCH & VERIFY
    const merchant = await storage.getMerchant(id);
    if (!merchant) {
      return res.status(404).json({ message: "Merchant not found" });
    }

    // 3. IDOR CHECK (Does the header key match the owner of the ID?)
    // Note: In production, this header should come from a secure Gateway or Worker, not client-side.
    if (merchant.stripeConnectId !== authHeader && merchant.id !== id) { 
        // Double check: strict ownership
       log(`Security Alert: Unauthorized update attempt for merchant ${id}`, 'security');
       return res.status(403).json({ message: "Forbidden" });
    }

    // 4. SAFE UPDATE
    const { billingCountry, billingAddress, email } = req.body;
    const updateData: any = {};
    if (billingCountry !== undefined) updateData.billingCountry = billingCountry;
    if (billingAddress !== undefined) updateData.billingAddress = billingAddress;
    if (email !== undefined) updateData.email = email;

    if (Object.keys(updateData).length === 0) {
      return res.status(400).json({ message: "No valid fields to update" });
    }

    const updated = await storage.updateMerchant(id, updateData);
    res.json(updated);
  } catch (error) {
    const msg = error instanceof Error ? error.message : String(error);
    log(`Update error: ${msg}`, 'error');
    res.status(500).json({ message: "Internal Server Error" });
  }
});
3. Verification
Run ls -la migrations/ to prove 0001_add_billing_fields.sql exists.

Restart the server.